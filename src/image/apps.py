from django.apps import AppConfig


class ImageConfig(AppConfig):
    """Configuration for the image app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'image'
    verbose_name = 'Image'
